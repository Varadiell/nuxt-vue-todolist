<script setup lang="ts">
import type { NumberFieldIncrementProps } from 'radix-vue'
import { cn } from '@/lib/utils'
import { Plus } from 'lucide-vue-next'
import { NumberFieldIncrement, useForwardProps } from 'radix-vue'
import { computed, type HTMLAttributes } from 'vue'

const props = defineProps<NumberFieldIncrementProps & { class?: HTMLAttributes['class'] }>()

const delegatedProps = computed(() => {
  const { class: _, ...delegated } = props

  return delegated
})

const forwarded = useForwardProps(delegatedProps)
</script>

<template>
  <NumberFieldIncrement data-slot="increment" v-bind="forwarded" :class="cn('tw-absolute tw-top-1/2 tw--translate-y-1/2 tw-right-0 disabled:tw-cursor-not-allowed disabled:tw-opacity-20 tw-p-3', props.class)">
    <slot>
      <Plus class="tw-h-4 tw-w-4" />
    </slot>
  </NumberFieldIncrement>
</template>
